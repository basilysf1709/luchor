# Agent service bind address
HOST=0.0.0.0
PORT=8787

# Required for Anthropic model calls
ANTHROPIC_API_KEY=

# Optional: require bearer auth between web -> agent service
AGENT_SERVICE_API_KEY=

# Optional: Composio integration for external tool access (Gmail, Slack, etc.)
COMPOSIO_API_KEY=
